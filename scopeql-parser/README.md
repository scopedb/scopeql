# ScopeQL Parser

This repository contains the ScopeQL Parser, a tool designed to parse ScopeQL queries and commands. ScopeQL is a query language used for querying and manipulating data stored in ScopeDB.
